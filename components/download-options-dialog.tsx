import { Download, X } from 'lucide-react';

type Source = { id?: string; quality?: string; resolution?: string; size?: string };

export function DownloadOptionsDialog({ slug, title, sources }: { slug: string; title: string; sources: Source[] }) {
  const options = sources.filter((source) => /^(720p|1080p)$/i.test(source.quality || source.resolution || '')).slice(0, 2);
  return <details className="group relative"><summary className="flex min-h-12 cursor-pointer list-none items-center justify-center gap-2 rounded-full border border-white/20 px-6 py-3.5 text-sm font-semibold"><Download size={17} /> Download</summary><div className="absolute bottom-full left-1/2 z-20 mb-3 w-[min(22rem,calc(100vw-2rem))] -translate-x-1/2 rounded-2xl border border-white/15 bg-[#242620] p-5 shadow-2xl"><div className="flex items-center justify-between"><h2 className="font-serif text-2xl">Download {title}</h2><X size={16} className="text-white/40" /></div><p className="mt-1 text-xs text-white/50">Choose a quality</p><div className="mt-4 grid gap-2">{options.map((source) => { const quality = (source.quality || source.resolution || '').toLowerCase(); return <a key={source.id || quality} href={`/api/download/resolve?slug=${encodeURIComponent(slug)}&quality=${encodeURIComponent(quality)}`} className="flex items-center justify-between rounded-xl border border-white/10 px-4 py-3 text-sm hover:bg-white/10"><span>{quality} <span className="text-white/55">• {source.size || 'Size unavailable'}</span></span><Download size={15} /></a>; })}{!options.length && <p className="rounded-xl bg-white/5 px-4 py-3 text-sm text-white/55">No 720p or 1080p file is ready yet.</p>}</div></div></details>;
}
