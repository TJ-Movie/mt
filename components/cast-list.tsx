import { UserRound } from 'lucide-react';
type CastMember = string | { actor: string; character?: string; image?: string; profileUrl?: string; profileR2Key?: string };
export function CastList({ cast }: { cast: CastMember[] }) { return <section className="mx-auto max-w-[1380px] px-5 pb-10 sm:px-8 lg:px-12"><p className="section-kicker text-[#ef796d]">Cast & characters</p><div className="mt-5 flex gap-4 overflow-x-auto pb-3">{cast.map((member, index) => { const actor = typeof member === 'string' ? member : member.actor; const character = typeof member === 'string' ? '' : member.character; const image = typeof member === 'string' ? '' : (member.profileR2Key ? '/media/' + member.profileR2Key : member.image?.trim() || member.profileUrl?.trim()); return <article key={`${actor}-${index}`} className="w-32 shrink-0 text-center"><div className="mx-auto flex size-24 items-center justify-center rounded-full border border-white/20 bg-[radial-gradient(circle_at_35%_25%,rgba(239,121,109,.28),rgba(255,255,255,.06)_52%,rgba(0,0,0,.2))] p-1 shadow-[0_0_0_4px_rgba(255,255,255,.03)]">{image ? <img src={image} alt="" className="h-full w-full rounded-full object-cover" /> : <span aria-label={`${actor} avatar placeholder`} className="flex h-full w-full items-center justify-center rounded-full border border-white/10 bg-black/15"><UserRound className="text-white/45" size={30} strokeWidth={1.5} /></span>}</div><p className="mt-3 truncate text-sm font-semibold" title={actor}>{actor}</p>{character ? <p className="truncate text-xs text-white/45" title={character}>{character}</p> : null}</article>; })}</div></section>; }
