'use client';
import { Plus, Trash2 } from 'lucide-react';
import { Button } from '../ui/button';
import { Input } from '../ui/input';
import { Textarea } from '../ui/textarea';
import type { AdminMovie } from '../../db';
type Episode = NonNullable<AdminMovie['episodes']>[number];
export function EpisodeBuilder({ episodes, onChange }: { episodes: Episode[]; onChange: (episodes: Episode[]) => void }) {
  function update(index: number, key: keyof Episode, value: string) { onChange(episodes.map((episode, itemIndex) => itemIndex === index ? { ...episode, [key]: key === 'season' || key === 'episode' ? Math.max(1, Number(value) || 1) : value } : episode)); }
  return <div className="space-y-3">{episodes.map((episode, index) => <div key={`${index}-${episode.season}-${episode.episode}`} className="space-y-3 rounded-xl border border-white/10 bg-black/10 p-4"><div className="grid gap-3 md:grid-cols-[100px_100px_1fr_1fr_auto]"><label><span className="mb-1 block text-xs text-white/40">Season</span><Input type="number" min={1} value={episode.season} onChange={(event) => update(index, 'season', event.target.value)}/></label><label><span className="mb-1 block text-xs text-white/40">Episode</span><Input type="number" min={1} value={episode.episode} onChange={(event) => update(index, 'episode', event.target.value)}/></label><label><span className="mb-1 block text-xs text-white/40">Title</span><Input value={episode.title} onChange={(event) => update(index, 'title', event.target.value)} placeholder="Episode title"/></label><label><span className="mb-1 block text-xs text-white/40">Stream link</span><Input value={episode.url ?? ''} onChange={(event) => update(index, 'url', event.target.value)} placeholder="https://..."/></label><Button type="button" variant="ghost" className="self-end text-red-300" onClick={() => onChange(episodes.filter((_, itemIndex) => itemIndex !== index))} aria-label={`Remove episode ${episode.season}-${episode.episode}`}><Trash2/></Button></div><div className="grid gap-3 md:grid-cols-2"><label><span className="mb-1 block text-xs text-white/40">Thumbnail / poster URL</span><Input value={episode.thumbnail ?? ''} onChange={(event) => update(index, 'thumbnail', event.target.value)} placeholder="https://.../episode.jpg"/></label><label><span className="mb-1 block text-xs text-white/40">Rating / score</span><Input type="number" min={0} max={10} step="0.1" value={episode.rating ?? ''} onChange={(event) => update(index, 'rating', event.target.value)}/></label><label className="md:col-span-2"><span className="mb-1 block text-xs text-white/40">Episode overview</span><Textarea value={episode.description ?? ''} onChange={(event) => update(index, 'description', event.target.value)} placeholder="Short episode description" className="min-h-20"/></label><label className="md:col-span-2"><span className="mb-1 block text-xs text-white/40">Download mirrors (one URL per line)</span><Textarea value={(episode.downloadSources ?? []).map((source) => `${source.label} | ${source.quality} | ${source.resolution} | ${source.size} | ${source.url}`).join('\n')} onChange={(event) => update(index, 'downloadSources', event.target.value.split('\n').map((line, mirrorIndex) => { const parts = line.split('|').map((part) => part.trim()); if (parts.length === 1 && /^https:\/\//i.test(parts[0] ?? '')) return { label: `Download ${mirrorIndex + 1}`, quality: 'Standard', resolution: 'Auto', size: 'Unknown', url: parts[0] }; const [label, quality, resolution, size, ...url] = parts; return { label: label ?? '', quality: quality ?? '', resolution: resolution ?? '', size: size ?? '', url: url.join('|') }; }).filter((source) => source.url))} placeholder="Server 1 | WEB-DL | 1080p | 1.2 GB | https://..." className="min-h-20"/></label></div></div>)}<Button type="button" variant="outline" onClick={() => onChange([...episodes, { season: 1, episode: episodes.length + 1, title: '', url: undefined }])}><Plus/> Add episode</Button><p className="text-xs text-white/40">Episode metadata is saved in the existing episodes JSON field; old records remain compatible.</p></div>;
}
